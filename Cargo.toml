[package]
name = "oxide"
version = "0.1.0"
edition = "2024"
description = "A tiny POSIX-ish shell written in Rust, built as a learning project."

[dependencies]
anyhow = "1.0.102"
serde = { version = "1.0.228", features = ["derive"] }
toml = "1.1.2"
